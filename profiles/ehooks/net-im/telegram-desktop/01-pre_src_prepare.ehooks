# Autostart delay #

ehooks() {
	local f=lib/xdg/org.${PN/-/.}

	# Obsolete filename #
	[[ -e lib/xdg/${PN/-}.desktop ]] && f=lib/xdg/${PN/-}

	sed -i \
		-e "/SingleWindow/a X-GNOME-Autostart-Delay=3" \
		${f}.desktop || die
}
